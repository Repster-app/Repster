# Changelog

Versioning rule: the app marketing version uses `0.x`, and each release bumps `x` by 1.

## 0.1 (2026-03-24)
Switched the app to `0.x` versioning and added a release script that bumps the version and prepends a one-sentence changelog entry.
